const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	darkMode: 'class',
	theme: {
		extend: {
			fontFamily: {
				poppins: ["Poppins", "cursive"],
		},
	},

	plugins: []
	},
};

module.exports = config;
